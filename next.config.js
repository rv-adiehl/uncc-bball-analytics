/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Explicitly enable CSS support (should be default but making it explicit)
  experimental: {},
};

module.exports = nextConfig;
